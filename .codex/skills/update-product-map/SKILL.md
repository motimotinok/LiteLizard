---
name: update-product-map
description: "LiteLizard の `docs/product-map.md` を更新するスキル。ユーザーが「product-map更新して」「全体像に反映して」「思いつきを整理して」「将来テーマとして残して」「この構想を地図に入れて」などと依頼したときに使う。実装キューや完了履歴ではなく、プロダクト全体像・将来方向・チケット化前の検討テーマを整理する。"
---

# Update Product Map

`docs/product-map.md` を、LiteLizard の全体像・将来方向・チケット化前の検討テーマの地図として更新する。

## 使う場面

- 思いつきや将来構想を、まだ実装チケットにせず整理したい
- 次にやる候補や人間が判断するテーマを product-map に反映したい
- GitHub Issue や会話中の構想を product-map に要約したい
- product-map が古くなったので、現状に合わせて棚卸ししたい

使わない場面:

- すぐ実装できる作業を作る場合（`create-ralph-ticket`）
- 完了した作業を履歴に残す場合（`update-changelog`）
- 確定仕様を詳細化する場合（`spec-writer`）
- GitHub Issue を新規作成する場合（`create-github-issue`）

## 成果物

- `docs/product-map.md`

必要がなければ他のファイルは更新しない。

## 成功条件

- product-map が、現在の開発方針・将来方向・判断テーマを短く表している
- 細かい進捗、完了履歴、実装手順が混ざっていない
- 実装に移すべきものと、まだ判断が必要なものが区別されている
- `docs/tickets/`、`CHANGELOG.md`、`docs/specs/`、`docs/decisions.md` との役割が重複していない
- 不要な全面書き換えや文体統一だけの差分が入っていない

## 制約

- product-map を WBS やタスク台帳にしない
- チケット単位の status、担当、期限、完了チェックを持たせない
- 完了履歴は `CHANGELOG.md` に任せる
- 実装キューは `docs/tickets/` に任せる
- 確定仕様の詳細は `docs/specs/` と `docs/decisions.md` に任せる
- GitHub Issue を現役実装キューとして扱わない
- 曖昧な構想を勝手に実装チケット化しない

## 更新方針

既存構成をできるだけ保ち、必要なセクションだけ最小差分で更新する。

基本の置き場所:

- プロダクトの核: 変えない前提の大きな価値・体験
- 現在おおむね整っている領域: 完了済みの大きな機能領域
- 今後の方向: 伸ばしたい方向性
- 人間が判断するテーマ: 仕様判断・優先順位・プロダクト判断が必要なもの
- GitHub Issue に残っている将来検討: 外部参照や長期構想として残す Issue
- チケット化の基準: 運用ルール

思いつきの扱い:

- すぐ実装できるなら product-map ではなく `create-ralph-ticket` を提案する
- まだ曖昧なら「今後の方向」または「人間が判断するテーマ」に短く入れる
- 外部参照や長期構想として残す必要があるなら GitHub Issue 化を提案する

## 完了時の報告

更新したセクションと、product-map に置いた理由を短く報告する。チケット化した方がよい具体項目が見つかった場合は、勝手に作らず候補として示す。
