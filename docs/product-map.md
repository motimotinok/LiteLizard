# LiteLizard Product Map

最終更新: 2026-05-07

このファイルは、LiteLizard の全体像と今後伸ばしたい方向を LLM と人間が共有するための地図。作業台帳ではないため、細かい完了状態は持たない。実装可能な作業は `docs/tickets/`、完了履歴は `CHANGELOG.md`、確定した仕様は `docs/specs/` と `docs/decisions.md` に置く。

LLM が次に何を見るか迷った時は、まずこのファイルを読む。ここで方向を確認し、実行対象は `docs/tickets/`、完了済みの事実は `CHANGELOG.md` と `docs/tickets/done/`、詳細仕様は `docs/specs/` と `docs/decisions.md` へ辿る。

## 現在の運用

- 実装キューは Ralph Loop チケット（`docs/tickets/*.md`）に一本化する。
- 完了済みチケットは `docs/tickets/done/` に残す。
- WBS は `docs/old/wbs.md` に退役済み資料として保存し、今後は更新しない。
- GitHub Issue は、将来構想・検討メモ・外部参照として必要な時だけ使う。
- `agent-ready` / `in-progress` による Issue 自律実行フローは退役済み。

## プロダクトの核

- ローカルファーストの Electron アプリとして、`.lzl` 文書を安全に読み書きする。
- 小説・長文制作向けに、章・段落・分析カードの対応関係を保つ。
- 読者視点の分析を、外部 API またはローカル LLM で実行できるようにする。
- 編集体験は、構造操作、検索、表示調整、分析結果の参照を自然に往復できる状態に保つ。

## 現在おおむね整っている領域

- `.lzl` フォーマット、documentId、保存モデル、プロジェクト管理
- Electron IPC / preload bridge / file service
- ローカル分析基盤、API キー保存、外部 API / Ollama 接続
- 段落分析、分析履歴、stale 表示、全体解析件数表示
- 章・段落の基本構造操作と Undo/Redo
- UI リデザイン後の検索、Recent files、Tweaks、ローカルフォント、分析カード連動表示

## 今後の方向

- Reading Agent を、読者プロファイル・プロンプト・分析実行に自然につながる中核体験へ育てる。
- 章単位・全体単位の分析を、段落分析の集約だけでなく実行体験として整える。
- 長文制作で迷子にならないための検索、要約、履歴、差分確認を強化する。
- ローカル LLM 体験を、接続できるだけでなく、モデル管理や失敗時の復旧まで含めて扱いやすくするか検討する。
- 拡張機能やカスタムワークフローは、ユーザーが自分の制作手順を増やせる入口として検討する。

## 人間が判断するテーマ

- Reading Agent の最初の完成形をどこまでにするか。
- 章解析・全体解析を「段落分析の集約」に留めるか、別 LLM 呼び出しとして設計するか。
- LiteLizard 管理のローカル LLM ランタイムを本当に持つか。
- ユーザー自作拡張やカスタムワークフローを、製品の主要導線に入れるか。
- クラウドログインやクラウド解析を、将来拡張として残すだけでよいか。

## GitHub Issue に残っている将来検討

- #72 LiteLizard 管理のローカル LLM ランタイムを検討する
- #73 ユーザー自作の拡張機能を追加できる入口を検討する
- #74 カスタムワークフローを自作ボタンとして実行できる仕組みを検討する
- #86 Reading Agent の temperature 設定を将来的に削除するか検討する

これらは現役の実装キューではなく、将来の方向性を検討するためのメモとして扱う。実装に落とす時は、必要に応じて `docs/tickets/` に小さな Ralph Loop チケットを作成する。

## チケット化の基準

- 実装・テスト・検証まで 1 つのループで扱えるなら `docs/tickets/` に置く。
- 仕様判断が先に必要なら、チケット化せず本ファイルの判断テーマに残す。
- 思いつきは、すぐ実装するなら `docs/tickets/`、まだ曖昧なら本ファイル、外部参照や長期構想として残したいなら GitHub Issue に置く。
- 本ファイルから「やる」と決めたものは、実装単位に分解して `docs/tickets/` に移す。本ファイル側では細かい進捗管理をしない。
- 完了後の履歴は `CHANGELOG.md` に残す。
- 設計判断として後から参照したいものは `docs/decisions.md` に残す。

## 関連スキル

- `update-product-map`: 思いつき・将来方向・判断テーマをこのファイルに整理する。
- `create-ralph-ticket`: 実装すると決めた内容を `docs/tickets/` に切り出す。
- `update-changelog`: 完了した変更を `CHANGELOG.md` に記録する。
