# 技術決定ログ (DECISIONS.md)

> **用途**: 技術選択の理由・却下した代替案・仕様との意図的な差異を記録する。
> 同じ議論を繰り返さないための記録。追記のみ行い、削除しない。
> フォーマット: 日付 / 決定内容 / 理由 / 却下した代替案

---

## [2026-05-12] プロジェクト復元の失敗時挙動と書き込み可否確認を明文化

- **決定**: 公開前仕様として、起動時の前回フォルダ復元・復元失敗時の整合処理・`.litelizard/` への書き込み可否確認を [`docs/specs/project-management.md`](specs/project-management.md) §5–§9 に固定する
- **挙動**:
  - 復元失敗時は `lastOpenedFolder` と `recentProjects` から失敗パスを除外し、ProjectSetupScreen で選び直しを促す
  - `ensureProject` と `listTree` IPC ハンドラで `assertProjectWritable` を呼び、`.litelizard/.write-probe-*` の書き込み・削除で書き込み可否を検出する
- **理由**: 削除済み / 権限のないフォルダを `lastOpenedFolder` に残したまま再起動を繰り返す状態を防ぎ、外部公開前にユーザーが詰まらない導線を保証するため
- **却下した案**:
  - 新規 IPC `clearLastOpenedFolder` を追加: `removeRecentProject` の振る舞い拡張で同等の整合が取れ、preload 契約を変えずに済む
  - `fs.access(litelizardDir, W_OK)` だけで書き込み可否を判定: macOS の TCC / ACL 下で誤検知が出やすいため、実プローブで確認する
- **参照**: `apps/desktop/src/main/projectManager.ts`, `apps/desktop/src/main/appStore.ts`, `apps/desktop/src/main/ipc.ts`, `apps/desktop/src/renderer/store/useAppStore.ts`

---

## [2026-05-05] Reading Agent を解析実行単位として採用 (R-18)

- **決定**: Reading Agent を `userData/agents.json` に保存するアプリ横断の読者プロファイルとし、新しい解析実行では `agentId` を正とする。`personaMode` は既存 `.lzl` / analysis 互換のため温存する
- **仕様**: [`docs/specs/reading-agent.md`](specs/reading-agent.md)
- **理由**: UI 上の読者選択、AgentsScreen の編集内容、main process の provider 実行を同じ契約に揃えないと、編集しても解析結果に反映されないため。agent をプロジェクト横断にすることで、作品をまたいだ読者キャラとして再利用できる
- **却下した案**:
  - `personaMode` の即時削除: 既存 `.lzl` と analysis 読み込み互換を壊すため
  - agent をプロジェクト単位保存にする: まずは読者キャラをアプリ横断の好みとして扱う方針にしたため

## [2026-04-25] UI 全面刷新（案 A Minimal を採用）

- **決定**: Claude Design (claude.ai/design) で詰めた **案 A Minimal**（iA Writer 寄り、極めて静謐な執筆 UI）を renderer に取り込む。仕様詳細は [`docs/specs/ui-redesign-minimal.md`](specs/ui-redesign-minimal.md)
- **構成変更点**:
  - レイアウト: `44px (icon rail) / 232px (sidebar) / 1fr (main)` のグリッドに刷新。メイン内に絶対配置タイトルバー + 中央寄せ表題 + 漢数字段落番号
  - macOS タイトルバー: `titleBarStyle: 'hiddenInset'` に変更し、トラフィックライト上に独自タイトルバーを重ねる（他 OS は `default` 維持）
  - 配色: 古紙黄ばみ (`oklch(98% 0.012 88)` 系) + 藍 (`oklch(40% 0.10 250)`) アクセント一色
  - タイポ: 本文は明朝固定 (Shippori Mincho) + UI フォントは system-ui ベース、メタは JetBrains Mono
  - ステータスバー（モード/視点/ヒント）と editor-footer（保存ドット + 文字数）を撤去
  - 分析パネル: Reading Agent ドロップダウン（モック 4 種）+ 主要ボタン「段落を読ませる」 + タグは下線のみ・色なし（emotion color hash を撤去）
- **理由**: 既存 UI は装飾過多で「機能のための場」になっていた。執筆という内向作業のための静かな UI を最優先するため、案 A の合意済み方針をそのまま採用
- **モック先行とした補助画面**: ウェルカムの最近リスト・分析エージェント管理画面は UI のみ実装し、永続化・プロンプト適用は別タスク (WBS 参照)
- **却下した案**:
  - 案 B Standard / 案 C Literary: 案 A の静謐さが本プロダクトのコンセプトに最も合うため
  - Tweaks 機能（明朝/ゴシック切替・本文サイズ・行間・黄ばみ強度・パネル横並び/オーバーレイ）の同時実装: スコープが広すぎるため後続タスクへ
  - フレームレス（`frame: false`）でトラフィックライトを完全自前描画: Win/Linux クロス OS 対応工数が増えすぎるため、`hiddenInset` で macOS のみカスタム化
- **参照**: `apps/desktop/src/main/main.ts`, `apps/desktop/src/renderer/styles.css`, `apps/desktop/src/renderer/index.html`, `apps/desktop/src/renderer/components/`

---

## [2026-04-13] 狭幅レイアウトでは固定パネル幅より縦積みを優先する (R-11)

- **決定**: renderer の狭幅時 (`1180px` 以下) は explorer / analysis パネルのインライン固定幅とドラッグリサイズを無効化し、CSS の縦積みレイアウトを優先する
- **理由**: media query による縦積み切り替えと JS 側の固定幅指定が競合すると、横スクロールやパネル食い込みが起きやすい。狭幅では幅調整の自由度よりレイアウト安定性を優先する
- **却下した案**:
  - 狭幅でも固定幅を維持したまま CSS だけで吸収する → inline style が優先され、崩れの根本原因を残す
  - 狭幅専用UIに大きく作り替える → R-11 の目的に対して変更範囲が広すぎる
- **参照**: `apps/desktop/src/renderer/App.tsx`, `apps/desktop/src/renderer/hooks/useResizablePanel.ts`, `apps/desktop/src/renderer/styles.css`

---

## [2026-03-05] GitHub Pages MVP はモックAPIでデモする

- **決定**: LLMなし・モックデータのみで動くデモを GitHub Pages に公開する
- **理由**: サーバー不要・コスト0・工数最小でデモが作れる。`createMockPreloadApi()` が完全実装済みだったため追加工数がほぼかからない
- **却下した案**: Vercel Edge Function 経由で実際のLLM APIを呼ぶ → 工数過大・コスト発生・APIキー管理が必要
- **参照**: `apps/desktop/src/preload/preloadMockApi.ts`

---

## [2026-02-xx] エディタに Lexical を採用

- **決定**: テキストエディタに Meta 製 Lexical v0.19.0 を採用
- **理由**: プラグインアーキテクチャで段落単位のノード管理が可能。React との相性が良く、カスタムDnDプラグインを組み込める
- **却下した案**:
  - ProseMirror: 学習コスト高・React との統合が複雑
  - Slate: メンテナンス停滞懸念
  - contenteditable 直接実装: DnD連携が複雑すぎる

---

## [2026-02-xx] DnD に @dnd-kit を採用

- **決定**: ドラッグ&ドロップに `@dnd-kit/core` + `@dnd-kit/sortable` を採用
- **理由**: React hooks ベースで Lexical カスタムプラグインとの統合が可能。アクセシビリティ対応済み
- **却下した案**: react-beautiful-dnd → メンテナンス終了

---

## [2026-02-xx] 状態管理に Zustand v5 を採用

- **決定**: アプリ全体の状態管理に Zustand v5 を使用
- **理由**: ボイラープレートが少なく、Electronレンダラーとの相性良好。スライス分割（documentOps.ts）で可読性を維持できる

---

## [2026-02-xx] ファイル形式を暫定 .md として運用（仕様差異）

- **決定**: 仕様上の `.lzl` ではなく、現状は `.md` ファイルを扱う
- **理由**: MVP フェーズでは形式策定より機能実装を優先する。`.lzl` の内部フォーマットは仕様§13が未策定
- **影響**: `titleFromPath()` が `.md` を除去する処理が入っている
- **将来**: `.lzl` フォーマット策定後に移行予定

---

## [2026-02-xx] クライアント側に API キー設定画面を実装（仕様差異）

- **決定**: 仕様§9「クライアント側に個別APIキー設定画面は持たない」に反して実装
- **理由**: 開発フェーズで各自のAPIキーで解析をテストするために必要だった
- **将来**: 本番ではサーバー認証（仕様§9の方針）に切り替える
- **注（S-09, 2026-03-30）**: ローカル完結が正式方針となり、クライアント側APIキー設定が正となった。「仕様差異」ではなくなった

---

## [2026-03-14] 分析機能の役割・製品構造を再定義

- **決定**: 分析ペインの役割を「読者の感想・評価」ではなく「書き手の意図が読者にどう届いたか（意図と解釈の擦り合わせ）」として定義する
- **理由**: 「面白かった」系の評価フィードバックは問題指摘マシンになりやすく、執筆体験を損なう。「この段落はこういう意図として読み取られる」という形式にすることで、書き手が自分で判断できる情報を渡せる
- **製品構造の整理**:
  - コア機能 = 書きやすさの追求（縦書き・横書き対応、執筆体験の洗練）
  - 分析 = 完成後レビューとして位置づけ（執筆中のリアルタイム分析ではない）
  - 分析機能はサブスク限定の付加価値として設計する
- **将来実装（MVPスコープ外）**:
  - 書き手の「意図宣言」フィールド（段落ごとに意図を記述）
  - 意図 vs 読者解釈のギャップ可視化
  - 感情アークのチャート表示（段落をまたいだ emotion タグの流れ）
- **却下した方向性**: 「問題点を指摘して書き直しを促すアシスタント」→ 書き手の主導権を損なうため採用しない
- **この決定が現在の実装に与える影響**: deepMeaning の文体方針（読者体感 + 書き手への気づきの混在）はこの定義と整合している

---

## [2026-03-20] 仕様ドキュメントをトピック別に分離 (docs/specs/)

- **決定**: 詳細仕様を `docs/specs/` 配下にトピック別ファイルとして管理する
- **理由**: decisions.md は「なぜ」のログであり仕様参照には不向き。spec_v003.md への追記は肥大化する。Claude/Codex 並列開発で仕様の参照先が明確である必要がある
- **運用ルール**:
  - `decisions.md`: 判断理由の要約 + `specs/` へのリンク
  - `docs/specs/*.md`: 現行仕様の詳細（実装者が参照する）
  - 相互リンクで追跡可能にする
- **却下した案**: 仕様書一本化（肥大化）、decisions.md 兼用（ログと仕様の性質が異なる）

---

## [2026-03-20] .lzl 内部フォーマット: フロントマター + インラインマーカー方式を採用 (S-01)

- **決定**: .lzl ファイルを YAML フロントマター + HTML コメント形式のインラインマーカー（`<!--:: ch/p {id} ::-->`）で構造化する。フォーマットバージョン `lzl-v1`
- **仕様**: `docs/specs/lzl-format.md`
- **理由**:
  - ID と本文が物理的に隣接 → 部分破損時の被害が局所化される（破損耐性の最優先原則）
  - 1ファイルで構造が完結する（自己記述性）。外部メタファイルに依存しない
  - 空行 = 段落境界ルールにより、マーカーが消失しても段落境界を推定可能
  - フロントマターに章・段落カウントを冗長データとして持ち、パース後に破損検出が可能
  - 章タイトルはマーカー内に1行完結で格納（`## 見出し` との誤認を防ぐ）
- **却下した案**:
  - ヘッダー集約方式: 本文と構造の物理的距離が大きく、部分破損時に構造全体が影響を受ける
  - ブロック区切り方式: 冗長性は高いが構文が重く、パーサーの複雑化に見合わない
  - メタデータ分離方式（.lzl + .lzl.meta）: 2ファイル同期の破綻リスクが破損耐性と矛盾。メタファイル喪失で全構造消失
- **前提条件（この前提が崩れたら再検討）**:
  - Git 管理しない
  - 段落内改行を許容しない
  - 外部エディタでの編集を想定しない（閲覧のみ）

---

## [2026-03-20] documentId 生成・重複検出の仕様決定 (S-03)

- **決定**: `d_/c_/p_` + `randomAlphanumeric(10)` に統一。プロジェクト起動時に全スキャンして重複検出、サイレントで再採番
- **仕様**: `docs/specs/document-id.md`
- **理由**: 外部ライブラリ不要（衝突確率 36^10 で十分）。`d_` は `{prefix}_` 形式で将来の衝突なし。重複はコピー時のみ発生するため通知不要
- **却下した案**: ファイルオープン時のみ検出（比較マップがない）、確認ダイアログ（ユーザー判断不要）

---

## [2026-03-21] IPC チャンネル定義と型安全ブリッジ設計 (E-01)

- **決定**: `BridgeApi` インターフェースと `IPC_CHANNELS` 定数マップを `@litelizard/shared` に配置し、main / preload / renderer の3層で共有する。preload は `ipcRenderer.invoke` による実 IPC ブリッジに切り替え
- **理由**: API 契約の Single Source of Truth を確立し、チャンネル名の typo を型レベルで防止する。ブラウザ dev モードでは `main.tsx` のモック fallback で動作するため、preload からモック依存を除去できる
- **却下した案**:
  - `apps/desktop/src/shared/` に配置: preload の tsconfig（rootDir=src/preload）からインポートできない
  - パラメータ/戻り値の完全型チェック付き registerHandler ラッパー: IPC 境界はシリアライズされるため実行時は any。過剰な型安全は誤った保証になる
- **参照**: `packages/shared/src/bridge.ts`, `apps/desktop/src/preload/ipcBridge.ts`

---

## [2026-03-27] 認証・セッションフロー設計 (S-05)

- **決定**:
  1. **ログイン方法**: OAuth のみ（メール+パスワード・マジックリンクは採用しない）
  2. **セッショントークン保存**: Electron `safeStorage` API を使用（macOS: Keychain、Windows: DPAPI、Linux: libsecret によるOS暗号化）
  3. **未ログイン時の挙動**: 分析パネルのみ変化。パネルを開くとログインボタン＋「ログインしてください」メッセージを表示。それ以外の機能（エディタ・ファイル操作）はログイン状態に関わらず動作する
- **理由**:
  - OAuth のみにすることでパスワード管理・ハッシュ保管の実装責任を排除できる
  - `safeStorage` は Electron 公式推奨の暗号化ストレージ。平文ファイルや localStorage より安全で、OS キーチェーン連携によりアプリ外からの読み取りを防止
  - 分析はサブスク限定機能（[2026-03-14] の製品構造決定と整合）なので、ゲートは分析パネルのみで十分
- **却下した案**:
  - メール+パスワード: パスワードハッシュ管理・リセットフロー実装コストが高い
  - マジックリンク: メール送信インフラが必要
  - localStorage へのトークン保存: XSS で窃取可能なため不採用
  - 全機能をログイン必須にする: エディタ単体の無料利用価値を損なうため不採用
- **仕様**: `docs/specs/auth-session.md`
- **影響範囲**: A-01〜A-05（認証・API 接続カテゴリ）がこの決定をベースに着手可能になる
- **未決定事項**: OAuth プロバイダー（Google / GitHub / 他）の選定は A-01 着手時に決定する

---

## [2026-03-28] 完全自動保存モデルを採用 (S-02)

- **決定**: 明示保存（Ctrl+S）を廃止し、完全自動保存に統一する。テキスト編集は 300ms デバウンスで即座に保存。DnD 並び替えは即確定だがテキスト編集より低優先度で保存キューに乗せる。dirty インジケータは表示しない
- **仕様**: `docs/specs/save-model.md`
- **理由**: ユーザーがタイピングと内容に集中したい。保存操作は面倒で意識したくないという明確な要望。デバウンス 300ms なら未保存を意識する時間窓が実質なく、dirty インジケータも不要になる
- **却下した案**:
  - 明示保存（Ctrl+S）: ユーザーが保存操作を意識する必要があり、執筆体験を損なう
  - 長めのデバウンス（2.5秒、現実装）: データ損失リスクが高く、ユーザーの「即座に保存されてほしい」要望と合わない

---

## [2026-03-28] フォルダベースのプロジェクト管理フロー (S-04)

- **決定**: Obsidian の Vault モデルを採用。初回起動時はフォルダ選択画面のみ表示し、選択されたフォルダに `.litelizard/` を自動生成してプロジェクトとする。2回目以降は前回フォルダを `electron-store` で記憶し直接開く。フォルダ切り替えはメニューから。複数フォルダ履歴は持たない
- **仕様**: `docs/specs/project-management.md`
- **理由**: フォルダ = プロジェクトというシンプルなモデルで、ユーザーが既存のファイル管理と自然に統合できる。独自のプロジェクトファイル形式を作るより学習コストが低い。`.litelizard/` をサブディレクトリに分けることで将来のファイル増加にも対応可能
- **却下した案**:
  - プロジェクトファイル方式（.litelizard ファイルを開く）: ファイル管理が二重になる
  - 最近開いたフォルダ一覧: 常に1フォルダで運用するため不要

---

## [2026-04-11] E-07 起動フローは renderer store 主導に統一

- **決定**: 初回起動・前回フォルダ復元・メニューからのフォルダ切り替えは、renderer の `useAppStore` に置く `restoreLastProject()` / `hydrateProject()` を共通入口にする。main プロセスはダイアログ実行とメニューイベント送信のみを担当する
- **理由**: 起動経路ごとに別々の状態更新を持つと `rootPath` / `document` / `dirty` の整合が崩れやすい。store 側に集約すると、復元失敗時の setup 画面フォールバックや将来の最近使ったプロジェクト履歴追加も同じ流れに乗せやすい
- **補足**:
  - メニューから renderer への逆方向通信は「フォルダ選択を要求する intent event」のみに留める
  - `electron-store` への新規依存追加は避け、既存の `appStore.ts` 自前 JSON ストアを継続利用する

---

## [2026-03-28] 解析 API エンドポイント仕様 (S-06)

- **決定**: 段落単位の分析を基本とし、複数段落をまとめて1リクエストで送信。レスポンスは SSE ストリーミングで段落ごとに逐次返却。コンテキストは対象段落より前の最大10段落。結果はローカルのみ保存（`.litelizard/analysis/{documentId}_{generation}.json`）し、世代管理で構造変更時にファイルを一新する。認証は OAuth またはユーザー自前 API キー（後者はログイン不要）の2系統
- **仕様**: `docs/specs/analysis-api.md`
- **理由**: サーバーをステートレスにすることでシンプルさとプライバシーを確保。SSE により長時間の一括分析でもユーザーが逐次結果を確認できる。コンテキスト上限10段落はトークンコストと分析精度のバランス。「読者は前から順に読む」ため後方段落は参照しない（製品コンセプトとの整合）
- **却下した案**:
  - 段落ごと個別リクエスト並列方式: リクエスト数過多、コンテキスト共有不可
  - 一括レスポンス方式: 章・全体分析時の待ち時間が長い
  - サーバー側で結果保存: 複雑化、プライバシーリスク

---

## [2026-03-30] 分析アーキテクチャをローカル完結に変更 (S-09)

- **決定**:
  1. 分析処理の主軸をローカル完結に変更。自社サーバー API への送信を前提としない
  2. 2系統の分析方式: 外部 API キー（OpenAI / Anthropic 等）+ ローカル LLM（Ollama 等）
  3. クラウドサーバーログイン（OAuth）は将来の拡張パスとして残す（実装練習も兼ねる）
  4. S-05（OAuth のみ）・S-06（サーバー側ループ処理）の前提を修正
- **理由**:
  - MVP 段階でのサーバー運用コスト・LLM プロバイダー契約の負担を排除
  - ユーザーが自分の API キーまたはローカル LLM で完結できる方がプライバシー・コスト面で優れる
  - ローカル完結→クラウド拡張は可能だが逆は難しい。まずローカルから始めるのが順序として正しい
- **却下した案**:
  - OAuth 完全廃止: ログイン実装の練習価値があり、将来のクラウド拡張パスを閉じたくない
  - クラウドサーバー主軸を維持: 個人開発では運用コストが大きく持続困難
- **S-05 への影響**: 「OAuth のみ」→「ローカル完結が主、OAuth は将来拡張パス」に変更。`docs/specs/auth-session.md` を改訂
- **S-06 への影響**: 「サーバー側ループ処理 + SSE」→「Electron main プロセスで処理 + IPC ストリーミング」に変更。`docs/specs/analysis-api.md` を改訂
- **WBS への影響**: 新カテゴリ「L: ローカル分析基盤」（L-01〜L-08）を追加。既存 A 系（認証・API 接続）は P3 に降格し「将来拡張」として残す
- **仕様 v003 への影響**: §1 概要・方針、§9 認証・API セクションをローカル完結に修正

---

## [2026-04-25] 分析コンテキストポリシー切替を将来タスク化 (L-09)

- **決定**: 段落解析のコンテキスト範囲と段落数上限を固定仕様にせず、将来 `document` / `chapter` と `none` / `lastN` を切り替えられる設定として扱う
- **仕様**: `docs/specs/analysis-api.md`
- **理由**: 最大10段落などの固定制限は、長い章・伏線・前章から続く読者体験を拾えず、分析UXを大きく下げる可能性がある。一方で、常に全文脈を渡す方式はコスト・速度・モデル上限のリスクがあるため、ユーザーが作品や分析目的に応じて選べる方がよい
- **却下した案**:
  - 常に章内のみ: 章をまたいで続く読者体験を拾えない
  - 常に文書全体・上限なし: 長文でコスト・速度・モデル上限に当たりやすい
  - T-05 に即時実装として混ぜる: 設定 UI・保存形式・テスト範囲まで広がり、テスト品質タスクの範囲を超える

---

## [2026-04-02] 章削除・段落統合エッジケース仕様 (S-07)

- **決定**: 章削除時は段落を前の章末尾に吸収。先頭章・最後の1章の削除時は「無題の章」を生成して段落を保持。Backspace による段落統合は同一章内のみ（章をまたがない）。空章は許容。確認ダイアログなし
- **仕様**: `docs/specs/chapter-paragraph-ops.md`
- **理由**: シンプルさを優先。章境界を Backspace で越えられると意図しない構造変更が起きやすい。確認ダイアログは将来の Undo/Redo（S-08）で代替する。空章を許容することで DnD 操作の自由度を確保
- **却下した案**:
  - 先頭章削除時に次の章へ吸収: 「前の章に吸収」ルールと一貫しない
  - 章をまたぐ Backspace 統合: 意図しない章境界の消失リスク
  - 空章の自動削除: ユーザーが意図的に空章を残すケースを阻害

---

## [2026-04-03] Undo/Redo 対象範囲と実装方針 (S-08)

- **決定**: 全操作（テキスト編集・章削除・段落統合/分割・DnD 並び替え）を Undo/Redo 対象とし、Zustand 統合 Command スタックで一元管理する
- **仕様**: `docs/specs/undo-redo.md`
- **理由**: Lexical HistoryPlugin（テキスト編集のみ）と構造操作（Zustand）の2系統が分裂すると Ctrl+Z の挙動が不整合になる。Lexical editorState はシリアライズ可能（`toJSON`/`parseEditorState`）なので、Zustand document スナップショットとセットで保存・復元する統合スタックに一本化できる
- **却下した案**:
  - Lexical HistoryPlugin を維持 + 構造操作を別スタック管理: Ctrl+Z の優先順位制御が複雑になり、テキスト編集と構造操作の間で Undo 順序が保証できない
- **テキスト編集の粒度**: Lexical デフォルトのグループ単位を踏襲（1打鍵ごとではなくグループ化された単位で Undo）

---

## [2026-02-xx] EditorPane をコンポーネント分割（editor/ ディレクトリ）

- **決定**: 旧 `EditorPane.tsx` を `components/editor/` 配下に分割
- **理由**: ファイルが肥大化し、Lexicalプラグイン・DnD・マクロビューが1ファイルに混在していた
- **構成**:
  - `index.tsx` (シェル) / `MicroEditorView.tsx` / `MacroView.tsx`
  - `plugins/` (DragHandlePlugin等) / `utils/` (ids, nodeKeyMapping等)
